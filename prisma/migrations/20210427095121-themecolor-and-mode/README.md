# Migration `20210427095121-themecolor-and-mode`

This migration has been generated by Jussi at 4/27/2021, 12:51:21 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "users" DROP COLUMN "prefers_color_scheme",
ADD COLUMN     "theme_color" TEXT,
ADD COLUMN     "theme_mode" TEXT
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210420094130-log-optional-user..20210427095121-themecolor-and-mode
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource postgresql {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Transaction {
   id            String   @id @default(uuid())
@@ -23,22 +23,23 @@
   @@map(name: "transactions")
 }
 model User {
-  id                 String   @id @default(uuid())
-  displayName        String?  @map("display_name")
-  email              String?  @unique
-  emailVerified      Boolean  @default(false) @map("email_verified")
-  password           String?
-  googleId           String?  @unique @map("google_id")
-  photoUrl           String?  @map("photo_url")
-  tokenVersion       Int      @default(1) @map("token_version")
-  disabled           Boolean  @default(false)
-  isAdmin            Boolean  @default(false) @map("is_admin")
-  prefersColorScheme String?  @map("prefers_color_scheme")
-  createdAt          DateTime @default(now()) @map("created_at")
-  updatedAt          DateTime @default(now()) @updatedAt @map("updated_at")
-  stripeCustomerId   String?  @map("stripe_customer_id")
+  id               String   @id @default(uuid())
+  displayName      String?  @map("display_name")
+  email            String?  @unique
+  emailVerified    Boolean  @default(false) @map("email_verified")
+  password         String?
+  googleId         String?  @unique @map("google_id")
+  photoUrl         String?  @map("photo_url")
+  tokenVersion     Int      @default(1) @map("token_version")
+  disabled         Boolean  @default(false)
+  isAdmin          Boolean  @default(false) @map("is_admin")
+  themeColor       String?  @map("theme_color")
+  themeMode        String?  @map("theme_mode")
+  createdAt        DateTime @default(now()) @map("created_at")
+  updatedAt        DateTime @default(now()) @updatedAt @map("updated_at")
+  stripeCustomerId String?  @map("stripe_customer_id")
   Transactions Transaction[]
   Categories   Category[]
   Budgets      Budget[]
```


