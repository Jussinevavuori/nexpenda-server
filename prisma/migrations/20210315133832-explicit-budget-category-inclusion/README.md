# Migration `20210315133832-explicit-budget-category-inclusion`

This migration has been generated by Jussi at 3/15/2021, 3:38:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
ALTER TABLE "_BudgetToCategory" DROP CONSTRAINT "_BudgetToCategory_A_fkey"

ALTER TABLE "_BudgetToCategory" DROP CONSTRAINT "_BudgetToCategory_B_fkey"

CREATE TABLE "BudgetCategoryInclusion" (
    "budget_id" TEXT NOT NULL,
    "category_id" TEXT NOT NULL,

    PRIMARY KEY ("category_id","budget_id")
)

DROP TABLE "_BudgetToCategory"

ALTER TABLE "BudgetCategoryInclusion" ADD FOREIGN KEY("budget_id")REFERENCES "budgets"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "BudgetCategoryInclusion" ADD FOREIGN KEY("category_id")REFERENCES "categories"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20210315114915-add_category_unique_uid_id..20210315133832-explicit-budget-category-inclusion
--- datamodel.dml
+++ datamodel.dml
@@ -3,9 +3,9 @@
 }
 datasource postgresql {
   provider = "postgresql"
-  url = "***"
+  url = "***"
 }
 model Transaction {
   id            String   @id @default(uuid())
@@ -53,25 +53,35 @@
   integerAmount Int      @map("integer_amount")
   createdAt     DateTime @default(now()) @map("created_at")
   updatedAt     DateTime @default(now()) @updatedAt @map("updated_at")
-  Categories Category[]
+  BudgetCategoryInclusion BudgetCategoryInclusion[]
   User User @relation(fields: [uid], references: [id])
   @@map(name: "budgets")
 }
+model BudgetCategoryInclusion {
+  budgetId   String @map("budget_id")
+  categoryId String @map("category_id")
+
+  Budget   Budget   @relation(fields: [budgetId], references: [id])
+  Category Category @relation(fields: [categoryId], references: [id])
+
+  @@id([categoryId, budgetId])
+}
+
 model Category {
   id        String   @id @default(uuid())
   uid       String
   value     String
   icon      String?
   createdAt DateTime @default(now()) @map("created_at")
   updatedAt DateTime @default(now()) @updatedAt @map("updated_at")
-  Transactions Transaction[]
-  Budgets      Budget[]
+  Transactions            Transaction[]
+  BudgetCategoryInclusion BudgetCategoryInclusion[]
   User User @relation(fields: [uid], references: [id])
   @@unique([uid, value], name: "unique_uid_value")
```


